{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "framework": "vite",
  "rewrites": [
    { "source": "/story/:id", "destination": "/api/og?id=$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
