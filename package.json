{
  "scripts": {
    "build": "npm --prefix server install && npm --prefix server run build && rm -rf dist node_modules && cp -r server/dist dist && cp -r server/node_modules node_modules",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.59.1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1075.0"
  }
}
